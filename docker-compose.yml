version: '3.1'

services:
  tijdloze-db:
    image: stijnvermeeren/tijdloze-db
    environment:
      MYSQL_ROOT_PASSWORD: secret
  tijdloze-api:
    image: stijnvermeeren/tijdloze-api
    ports:
      - "9000:9000"

